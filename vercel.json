{
  "version": 2,
  "installCommand": "pnpm install",
  "buildCommand": "pnpm run build",
  "framework": "nextjs",
  "regions": ["bom1"],
  "functions": {
    "app/**/*.js": {
      "maxDuration": 10,
      "memory": 1024
    },
    "app/api/**/*.js": {
      "maxDuration": 10,
      "memory": 1024
    }
  },
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/static-build",
      "config": {
        "maxLambdaSize": "50mb"
      }
    }
  ]
}